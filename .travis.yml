language: java
jdk:
  - openjdk17

branches:
  only:
    - main

# Travis CI 서버의 Home
cache:
  directories:
    - $HOME/.m2/repository
    - $HOME/.gradle

script:
  - ./gradlew clean build

# CI 실행 완료 시 메일로 알람
notifications:
  email:
    recipients:
      - komj36@gmail.com
      -
# 재 빌드를 위한 push 주석 (추후 삭제)
